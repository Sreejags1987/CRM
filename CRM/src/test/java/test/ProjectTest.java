package test;

import org.testng.annotations.Test;

import page.LoginPage;
import page.NotePage;
import page.ProjectPage;

import org.testng.annotations.BeforeTest;
import org.openqa.selenium.WebDriver;
import org.testng.Assert;
import org.testng.annotations.AfterTest;

public class ProjectTest extends BaseTest{
	
 /* @Test(priority=1,groups = { "smoke", "regression" })
    public void verifyCreateProject()throws Exception{
  
      LoginPage lg=new LoginPage(driver);
      lg.doLogin("admin@admin.com", "12345678");
      String expected = "Dashboard";
	  Assert.assertEquals(actual, expected);
  
      ProjectPage pp=new ProjectPage(driver);
      pp.clickOnProject();
      String actual = pp.doCreateProject("Sreejas CRM Project","Description of CRM Project");
      String expected = "Sreejas CRM Project";
      Assert.assertEquals(actual, expected);
      System.out.println("Test 6: The project is created after login");
  }

  
  //@Test(priority=2, groups = { "regression" })
    public void verifySearchProject()throws Exception{
	  
	  LoginPage lg=new LoginPage(driver);
	  lg.doLogin("admin@admin.com", "12345678");
	  
	  ProjectPage pp=new ProjectPage(driver);
	  pp.doCreateProject(null, null);
	  String actual = pp.doSearchProject("This is a test");
	  String expected = "This is a test";
	  Assert.assertEquals(actual, expected);
	  System.out.println("Test 7 : The project is created after login and get searched ");
	  
  }
  //@Test(priority=3, groups = { "smoke", "regression" })
    public void verifyEditProject()throws Exception{
	  
	  LoginPage lg=new LoginPage(driver);
	  lg.doLogin("admin@admin.com", "12345678");
	  
	  ProjectPage pp=new ProjectPage(driver);
	  pp.doCreateProject();
	  pp.doEditproject();*/
	  
	  
	  
  
	  
	  
	  
	  
  
  
  


}
